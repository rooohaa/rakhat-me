import React from 'react';

import { Layout } from '../app/components';

const ContactPage = () => {
   return (
      <Layout>
         <p>Contact page</p>
      </Layout>
   );
};

export default ContactPage;
