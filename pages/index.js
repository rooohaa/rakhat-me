import * as React from 'react';

import { Layout } from '../app/components';

const HomePage = () => {
   return (
      <Layout>
         <p>Main page</p>
      </Layout>
   );
};

export default HomePage;
